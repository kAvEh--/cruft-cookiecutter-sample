[project]
name = "multiple-template-project"
version = "0.1.0"
description = "Community cookiecutter template for multiple Python project types"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cookiecutter>=2.6.0",
    "ruff>=0.12.7",
    "typer>=0.20.0",
]